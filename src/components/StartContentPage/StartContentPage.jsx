import React from 'react';
import styles from './StartContentPage.module.css';

const StartContentPage = (props) => {
    return (
        <div className={styles.content}>
            start page
        </div>
    )
}

export default StartContentPage;